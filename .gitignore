# Rust
/target
/src-tauri/target
/src-tauri/gen

# Node
node_modules
dist
dist-ssr
*.local

# Editor / OS cruft
.vscode/*
!.vscode/extensions.json
.DS_Store

# --- Data backstop ---
# Real entries must never live in this repo — the actual defence is that
# saijiki's storage code only ever writes to the OS app-data directory.
# These patterns exist only to catch a mistake, not to be relied on.
kigo/
**/kigo/
saijiki.json
**/saijiki.json
**/store/*.md

# Claude Code local settings
.claude/settings.local.json
